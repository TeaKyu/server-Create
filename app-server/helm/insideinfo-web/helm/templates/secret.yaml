{{- if .Values.secret }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "insideinfo-web.fullname" . }}-secret
  labels:
    {{- include "insideinfo-web.labels" . | nindent 4 }}
stringData:
  {{- toYaml .Values.secret.data | nindent 2 }}
{{- end }}
