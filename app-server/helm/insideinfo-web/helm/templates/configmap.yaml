apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "insideinfo-web.fullname" . }}-properties
  labels:
    {{- include "insideinfo-web.labels" . | nindent 4 }}
data:
  {{- toYaml .Values.configmap.data.properties | nindent 2 }}
