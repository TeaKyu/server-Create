# 05. K3s 전체 스택 사용법 가이드

## 설치된 컴포넌트 전체 목록

| # | 컴포넌트 | 네임스페이스 | 설치 방식 | 역할 |
|---|---------|------------|----------|------|
| - | Flannel | kube-system | K3s 내장 | Pod 네트워크 (CNI) |
| 1 | Helm 4.x | - | 바이너리 | 패키지 매니저 |
| 2 | MetalLB | metallb-system | Helm | 온프레미스 LoadBalancer |
| 3 | Envoy Gateway | envoy-gateway-system | Helm | Gateway API 기반 트래픽 라우팅 |
| 4 | cert-manager | cert-manager | Helm | TLS 인증서 자동 관리 |
| 5 | Headlamp | headlamp | Helm | 클러스터 웹 UI |
| 6 | Prometheus + Grafana | monitoring | Helm | 메트릭 모니터링 |
| 7 | Loki + Alloy | logging | Helm | 로그 수집/조회 |
| 8 | Sealed Secrets | kube-system | Helm | Secret 암호화 (GitOps) |
| 9 | Argo CD | argocd | Manifest | GitOps 배포 |
| 10 | Argo CD Image Updater | argocd | Manifest | 이미지 자동 업데이트 |

---

## 0. K3s 관리

### K3s 내장 컴포넌트

K3s가 자동으로 설치/관리하는 컴포넌트로, 별도 관리가 필요 없습니다.

| 컴포넌트 | 역할 |
|---------|------|
| containerd | 컨테이너 런타임 |
| Flannel | Pod 네트워크 (CNI) |
| CoreDNS | 클러스터 내부 DNS |
| Metrics Server | `kubectl top` 명령어 지원 |

### K3s 서비스 관리

```bash
systemctl status k3s              # 상태 확인
systemctl restart k3s             # 재시작
journalctl -u k3s -f --no-pager   # 로그 확인
```

### K3s 업그레이드

```bash
curl -sfL https://get.k3s.io | sh -s - server \
  --disable traefik \
  --disable servicelb \
  --write-kubeconfig-mode 644 \
  --tls-san "192.168.1.10" \
  --node-ip "192.168.1.10"
```

### K3s 완전 삭제

```bash
/usr/local/bin/k3s-uninstall.sh
```

---

## 1. MetalLB — 온프레미스 LoadBalancer

### 역할

온프레미스 환경에서 `type: LoadBalancer` 서비스에 External IP를 할당합니다.

### 상태 확인

```bash
kubectl get ipaddresspool -n metallb-system
kubectl get l2advertisement -n metallb-system
kubectl get svc -A | grep LoadBalancer
kubectl get pods -n metallb-system
```

### IP 대역 변경

```bash
kubectl edit ipaddresspool default-pool -n metallb-system
```

### 트러블슈팅

```bash
# External IP가 <pending>일 때
kubectl logs -n metallb-system -l app.kubernetes.io/component=controller --tail=20
kubectl get ipaddresspool -n metallb-system
```

---

## 2. Envoy Gateway — 외부 트래픽 진입점

### 확인

```bash
kubectl get gateway eg -o jsonpath="{.status.addresses[0].value}"
kubectl get httproute
```

### HTTPRoute 관리

```bash
kubectl apply -f my-httproute.yaml    # 추가
kubectl edit httproute <이름>          # 수정
kubectl delete httproute <이름>        # 삭제
```

### Helm 설정 변경

```bash
helm get values eg -n envoy-gateway-system
helm upgrade eg oci://docker.io/envoyproxy/gateway-helm \
  --version v1.7.1 -n envoy-gateway-system \
  --set <key>=<value>
```

---

## 3. cert-manager — TLS 인증서 자동 관리

### 확인

```bash
kubectl get clusterissuer
kubectl get certificate -A
```

### Gateway에 TLS 적용

```yaml
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: eg-default-tls
  namespace: default
spec:
  secretName: eg-default-tls
  issuerRef:
    name: selfsigned-issuer
    kind: ClusterIssuer
  dnsNames:
  - "*.local"
```

### Let's Encrypt로 전환

```yaml
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: letsencrypt-prod
spec:
  acme:
    server: https://acme-v02.api.letsencrypt.org/directory
    email: your-email@example.com
    privateKeySecretRef:
      name: letsencrypt-prod
    solvers:
    - http01:
        gatewayHTTPRoute:
          parentRefs:
          - name: eg
            namespace: default
```

---

## 4. Headlamp — 클러스터 웹 UI

### 접속

브라우저: `http://dashboard.local`

### 로그인 토큰

```bash
kubectl -n headlamp create token headlamp
```

### 수동 재설치 시 패치 필요

```bash
helm upgrade --install headlamp headlamp/headlamp \
  --namespace headlamp --set image.tag="v0.40.1"

kubectl -n headlamp patch deployment headlamp --type='json' -p='[
  {"op": "replace", "path": "/spec/template/spec/containers/0/args", "value": [
    "-in-cluster",
    "-in-cluster-context-name=main",
    "-plugins-dir=/headlamp/plugins"
  ]}
]'
```

---

## 5. Prometheus + Grafana — 메트릭 모니터링

### Grafana 접속

브라우저: `http://grafana.local` (admin / admin1234)

### 커스텀 ServiceMonitor 추가

```yaml
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: my-app-monitor
  namespace: monitoring
  labels:
    release: kube-prometheus-stack
spec:
  namespaceSelector:
    matchNames: [default]
  selector:
    matchLabels:
      app: my-app
  endpoints:
  - port: metrics
    interval: 30s
```

### 설정 변경

```bash
# Grafana 비밀번호 변경
helm upgrade kube-prometheus-stack prometheus-community/kube-prometheus-stack \
  -n monitoring --set grafana.adminPassword=새비밀번호

# Prometheus retention 변경
helm upgrade kube-prometheus-stack prometheus-community/kube-prometheus-stack \
  -n monitoring --set prometheus.prometheusSpec.retention=14d
```

---

## 6. Loki + Alloy — 로그 수집/조회

### 현재 구성

- **Loki**: SingleBinary + filesystem (emptyDir)
- **Alloy**: DaemonSet으로 Pod 로그 수집

> emptyDir이므로 Loki Pod 재시작 시 로그가 사라집니다.

### Grafana에 Loki 데이터소스 추가

1. Connections > Data Sources > Add > Loki
2. URL: `http://loki-gateway.logging.svc.cluster.local`
3. Save & Test

### LogQL 예시

```
{namespace="default"} |= "error"
{namespace="default"} | json | level="ERROR"
rate({namespace="default"}[5m])
```

### 상태 확인

```bash
kubectl get pods -n logging -l app.kubernetes.io/name=loki
kubectl get pods -n logging -l app.kubernetes.io/name=alloy
```

---

## 7. Sealed Secrets — GitOps Secret 암호화

### 사용법

```bash
# Secret 생성 (dry-run)
kubectl create secret generic db-credentials \
  --from-literal=username=admin \
  --from-literal=password=super-secret-pw \
  --dry-run=client -o yaml > db-secret.yaml

# 암호화
kubeseal --format yaml < db-secret.yaml > db-sealed-secret.yaml

# 적용
kubectl apply -f db-sealed-secret.yaml
```

### 공개키 백업 (중요!)

```bash
kubeseal --fetch-cert > sealed-secrets-cert.pem
```

---

## 8. Argo CD — GitOps 배포

### 접속

브라우저: `http://argocd.local`

```bash
# 초기 비밀번호 확인
kubectl -n argocd get secret argocd-initial-admin-secret \
  -o jsonpath="{.data.password}" | base64 -d; echo
```

### UI에서 Application 생성

1. **Applications** > **+ NEW APP**
2. **GENERAL**: Application Name, Sync Policy
3. **SOURCE**: Repository URL, Revision, Path (Chart.yaml이 있는 폴더)
4. **DESTINATION**: `https://kubernetes.default.svc`, Namespace
5. **CREATE**

### 배포 흐름

```
Git push → ArgoCD 감지 (3분 폴링) → 자동/수동 Sync → 클러스터 반영
```

### 설정 변경

```bash
# Sync 주기 변경
kubectl edit configmap argocd-cm -n argocd
# data.timeout.reconciliation: 60s

# TLS 활성화
kubectl patch configmap argocd-cmd-params-cm -n argocd --type merge \
  -p '{"data":{"server.insecure":"false"}}'
kubectl rollout restart deployment argocd-server -n argocd
```

---

## 9. Argo CD Image Updater — 이미지 자동 업데이트

### 사용법

```yaml
apiVersion: argocd-image-updater.argoproj.io/v1alpha1
kind: ImageUpdater
metadata:
  name: my-app-updater
  namespace: argocd
spec:
  applicationRefs:
  - namePattern: "my-app"
  images:
  - alias: "myapp"
    imageName: "your-registry/my-app:1.0"
    commonUpdateSettings:
      updateStrategy: semver
      versionConstraint: "~1.0"
```

### 로그 확인

```bash
kubectl -n argocd logs -l app.kubernetes.io/name=argocd-image-updater -f
```

---

## 유용한 명령어 모음

```bash
# 전체 Pod 상태
kubectl get pods -A

# 네임스페이스별 확인
kubectl get all -n monitoring
kubectl get all -n argocd
kubectl get all -n envoy-gateway-system

# HTTPRoute / Gateway
kubectl get httproute
kubectl get gateway eg

# 리소스 사용량
kubectl top nodes
kubectl top pods -A

# K3s 관리
systemctl status k3s
journalctl -u k3s -f --no-pager
```

---

## 서비스 접속 요약

| 서비스 | URL | 인증 |
|--------|-----|------|
| Grafana | http://grafana.local | admin / admin1234 |
| ArgoCD | http://argocd.local | admin / (secret에서 확인) |
| Headlamp | http://dashboard.local | 토큰 (create token) |
| Prometheus | http://prometheus.local | 없음 |
