# 03. 설치 단계별 가이드

## 전체 설치 흐름

```
[1단계] vagrant up                         → VM 3대 생성 (10~20분)
[2단계] k8s-common.sh × 3대                → OS 설정 + containerd + kubeadm (각 5~10분)
[3단계] k8s-master.sh × Master 1대          → 클러스터 초기화 + Calico (3~5분)
[4단계] k8s-worker.sh × Worker 2대          → 클러스터 조인 (각 1분)
[5단계] k8s-master-apps.sh × Master 1대     → 전체 DevOps 스택 설치 (10~15분)
[6단계] 확인                                → kubectl get nodes + Pod 상태
```

총 소요 시간: 약 30~50분 (인터넷 속도에 따라 다름)

> **왜 master.sh와 master-apps.sh를 나눴나요?**
> 워커 노드가 조인된 후에 스택을 설치해야 Pod들이 워커에 분산 스케줄링됩니다.
> 마스터만 있는 상태에서 스택을 설치하면 taint 때문에 Pod가 Pending에 걸리거나,
> taint를 해제해도 모든 부하가 마스터에 집중됩니다.

---

## 1단계 — VM 3대 생성

```bash
cd C:\k8s-lab
vagrant up
```

확인:
```bash
vagrant status
# master-node    running (virtualbox)
# worker-node1   running (virtualbox)
# worker-node2   running (virtualbox)
```

---

## 추가작업 : root 접속 가능하게 하기 → SSH 활성화 (운영에선 X)

각 VM에 접속해서 실행:
- vagrant ssh master-node
- vagrant ssh worker-node1
- vagrant ssh worker-node2

```
sudo -i
echo "root:1234" | chpasswd
sed -i 's/^#PermitRootLogin.*/PermitRootLogin yes/' /etc/ssh/sshd_config
sed -i 's/^PermitRootLogin.*/PermitRootLogin yes/' /etc/ssh/sshd_config
sed -i 's/^PasswordAuthentication no/PasswordAuthentication yes/' /etc/ssh/sshd_config
systemctl restart sshd
```

---

## 2단계 — 공통 스크립트 실행 (3대 모두)

각 VM에 접속해서 순서대로 실행합니다.

### Master
```bash
vagrant ssh master-node
sudo bash /vagrant/k8s-common.sh
# "공통 설정 완료" 출력 확인
exit
```

### Worker 1
```bash
vagrant ssh worker-node1
sudo bash /vagrant/k8s-common.sh
exit
```

### Worker 2
```bash
vagrant ssh worker-node2
sudo bash /vagrant/k8s-common.sh
exit
```

### k8s-common.sh가 하는 일 (순서대로)

| 단계 | 내용 | 설명 |
|------|------|------|
| [0-1] | root 비밀번호 | `root:1234` |
| [0-2] | 타임존 | Asia/Seoul, NTP 동기화 |
| [0-4] | 기본 패키지 | dnf-utils, iproute-tc, git |
| [0-5] | /etc/hosts | 3대 IP-호스트명 매핑 |
| [0-6] | 방화벽 해제 | firewalld 중지/비활성화 |
| [0-7] | Swap 비활성화 | kubelet 요구사항 |
| [0-8] | SELinux | Permissive 모드 |
| [1-1] | iptables | overlay, br_netfilter 모듈 + sysctl |
| [2] | containerd | Docker repo에서 최신 containerd 설치, cgroup systemd |
| [3] | kubeadm | pkgs.k8s.io v1.34 repo에서 kubelet/kubeadm/kubectl |
| [3-3] | 한글 | glibc-langpack-ko |

> 디스크 확장은 주석 처리되어 있습니다. `generic/rocky9` box는 128GB LVM 동적 디스크이므로 별도 확장이 필요 없습니다.

### 주요 명령어 설명

**`set -e`** — 스크립트 실행 중 어떤 명령이 실패하면 즉시 중단. 디버깅이 쉬워짐.

**iptables 세팅 (overlay, br_netfilter)** — Kubernetes Pod 간 통신에 필수. `br_netfilter`는 bridge 네트워크를 지나는 패킷이 iptables 규칙을 타도록 하고, `ip_forward`는 노드가 패킷을 다른 인터페이스로 전달할 수 있게 함.

**containerd cgroup systemd** — Kubernetes가 기본으로 systemd cgroup을 사용하므로 containerd도 맞춰줘야 함.

---

## 3단계 — Master 클러스터 초기화

```bash
vagrant ssh master-node
sudo bash /vagrant/k8s-master.sh
```

### k8s-master.sh가 하는 일

| 단계 | 컴포넌트 | 설치 방식 | 소요 시간 |
|------|---------|----------|----------|
| [4] | kubeadm init + Calico | Manifest | 3~5분 |
| [5] | kubectl 편의기능 | bashrc | 즉시 |

### 완료 시 출력

스크립트가 끝나면 다음 단계 안내가 출력됩니다.
**아직 다른 스크립트를 실행하지 마세요!** 먼저 워커를 조인합니다.

---

## 4단계 — Worker 조인

### 조인 토큰 확인 (Master에서)
```bash
kubeadm token create --print-join-command
```

출력 예시:
```
kubeadm join 192.168.56.30:6443 --token abcdef.0123456789abcdef \
  --discovery-token-ca-cert-hash sha256:xxxxxxxxxxxxxxxxxxxx
```

이 명령어 전체를 복사합니다.

### Worker 1
```bash
vagrant ssh worker-node1
sudo bash /vagrant/k8s-worker.sh "kubeadm join 192.168.56.30:6443 --token abcdef.0123456789abcdef --discovery-token-ca-cert-hash sha256:xxxx"
exit
```

### Worker 2
```bash
vagrant ssh worker-node2
sudo bash /vagrant/k8s-worker.sh "kubeadm join 192.168.56.30:6443 --token abcdef.0123456789abcdef --discovery-token-ca-cert-hash sha256:xxxx"
exit
```

> 주의: 조인 명령어는 반드시 따옴표(`"..."`)로 감싸야 합니다.

### 조인 확인 (Master에서)
```bash
kubectl get nodes
# NAME          STATUS   ROLES           AGE   VERSION
# k8s-master    Ready    control-plane   5m    v1.34.x
# k8s-worker1   Ready    <none>          1m    v1.34.x
# k8s-worker2   Ready    <none>          30s   v1.34.x
```

**3대 모두 Ready 상태를 확인한 후** 다음 단계로 진행합니다.

---

## 5단계 — 전체 DevOps 스택 설치

```bash
vagrant ssh master-node
sudo bash /vagrant/k8s-master-apps.sh
```

실행하면 먼저 `kubectl get nodes`로 노드 상태를 보여주고, Ready 확인을 물어봅니다.
`y`를 입력하면 설치가 시작됩니다.

### k8s-master-apps.sh가 하는 일 (순서대로)

| 단계 | 컴포넌트 | 설치 방식 | 소요 시간 |
|------|---------|----------|----------|
| [6] | Helm 4.x | 바이너리 | 30초 |
| [7] | MetalLB + IP Pool | Helm + CRD | 1~2분 |
| [8] | Envoy Gateway + GatewayClass | Helm + CRD | 1~2분 |
| [9] | cert-manager + ClusterIssuer | Helm + CRD | 1~2분 |
| [10] | Headlamp + Admin 계정 | Helm + RBAC | 1~2분 |
| [11] | kube-prometheus-stack | Helm | 2~3분 |
| [12] | Loki + Alloy | Helm | 2~3분 |
| [13] | Sealed Secrets + kubeseal CLI | Helm + 바이너리 | 1분 |
| [14] | Argo CD | Manifest (SSA) | 1~2분 |
| [15] | Image Updater | Manifest | 30초 |

### 완료 시 출력되는 접속 정보

스크립트 마지막에 모든 서비스의 접속 방법이 출력됩니다. 꼭 확인하세요.

---

## 6단계 — 설치 확인

Master에서 실행:

### 노드 상태 확인
```bash
kubectl get nodes
# NAME          STATUS   ROLES           AGE   VERSION
# k8s-master    Ready    control-plane   15m   v1.34.x
# k8s-worker1   Ready    <none>          10m   v1.34.x
# k8s-worker2   Ready    <none>          10m   v1.34.x
```

### 전체 Pod 상태 확인
```bash
kubectl get pods -A
# 모든 Pod가 Running 또는 Completed 상태여야 함
```

### 네임스페이스별 확인
```bash
kubectl get all -n metallb-system        # MetalLB
kubectl get all -n envoy-gateway-system  # Envoy Gateway
kubectl get all -n cert-manager          # cert-manager
kubectl get all -n headlamp               # Headlamp
kubectl get all -n monitoring            # Prometheus + Grafana
kubectl get all -n logging               # Loki + Alloy
kubectl get all -n argocd                # Argo CD + Image Updater

# Alloy DaemonSet 확인 (워커 수만큼 Pod가 있어야 함)
kubectl get pods -n logging -l app.kubernetes.io/name=alloy
```

### Envoy Gateway External IP 확인
```bash
kubectl get gateway eg -o jsonpath="{.status.addresses[0].value}"
# MetalLB가 할당한 192.168.56.200 같은 IP가 출력되어야 함
```

---

## 설치 실패 시 트러블슈팅

### kubeadm init 실패
```bash
# 초기화 후 재시도
kubeadm reset -f
rm -rf $HOME/.kube /etc/cni/net.d /etc/kubernetes/manifests/* /var/lib/etcd
sudo bash /vagrant/k8s-master.sh
```

### Helm 설치 실패 (timeout)
```bash
# 특정 Helm 릴리스만 재설치
helm uninstall <릴리스명> -n <네임스페이스>
# k8s-master-apps.sh에서 해당 단계만 복사해서 수동 실행
```

### Pod가 Pending 상태
```bash
kubectl describe pod <pod-name> -n <namespace>
# Events 섹션에서 원인 확인
# 흔한 원인: 리소스 부족, 이미지 Pull 실패, PV 없음
```

### Worker 조인 실패
```bash
# Worker에서 초기화 후 재시도
sudo kubeadm reset -f
# Master에서 새 토큰 생성
kubeadm token create --print-join-command
# 새 토큰으로 재조인
```
